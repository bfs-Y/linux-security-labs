# phase00-threat-modeling

Not started. Per the root README's own methodology, this phase should
happen before phase02 (authentication/SSH/PAM) and phase05 (mandatory
access control) get real content — everything downstream should be
justifiable against a written threat model, which doesn't exist yet.

Planned scope: asset inventory, trust boundaries, attacker profiles.
